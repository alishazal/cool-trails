<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Cool Trails - Search Results</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
  <div class="container mt-4">
    <h1>Search Results</h1>
    {{#if trails}}
      <div class="list-group">
        {{#each trails}}
          <a href="/trail/{{id}}" class="list-group-item list-group-item-action">
            <h5>{{name}}</h5>
            <p>{{description}}</p>
            {{#if shadeRating}}
              <p class="mb-1 text-muted">Shade Rating: {{shadeRating}}</p>
            {{/if}}
          </a>
        {{/each}}
      </div>
    {{else}}
      <p>No trails found for your query.</p>
    {{/if}}
    <a href="/home" class="btn btn-secondary mt-3">Back to Home</a>
  </div>
  <!-- Bootstrap Bundle with Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
